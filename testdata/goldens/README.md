# Goldens

Checked-in snapshots for LAB-5. Regenerated by:

```bash
go test ./... -update
```

| File | Source |
|------|--------|
| `mrmo-manifest.json` | `mrmo.Scan(testdata/fixtures/mrmo)` |
| `provider-manifest.json` | `provider.Scan(testdata/fixtures/provider)` |
| `compatibility-report.json` | matrix join of both fixtures |
| `explain-demo.txt` | explain output for ready / warning / blocked demo resources |
