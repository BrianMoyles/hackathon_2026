# Provider Fixtures

Put trimmed provider fixture inputs here for golden tests:

- provider registrar snippets
- resource exporter metadata snapshots
- resource permissions JSON snippets
- dependency tree JSON snippets
